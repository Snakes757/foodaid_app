[pytest]
# 1. Disable the broken 'pytest-twisted' plugin causing the crash
# 2. Add verbose output (-v)
addopts = -p no:twisted -v

# Configure asyncio mode to avoid warnings
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

# Ensure 'app' module is found without sys.path hacks
pythonpath = .